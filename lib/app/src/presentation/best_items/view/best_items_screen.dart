import 'package:flutter/material.dart';

class BestItemsScreen extends StatelessWidget {
  const BestItemsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Best Items"),
      ),
    );
  }
}